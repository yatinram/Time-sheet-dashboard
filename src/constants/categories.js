export const CATEGORIES = ['Development', 'Design', 'Client Meeting'];

// Single source of truth for category -> tag color, used in table tags,
// filter dropdown, and the form's Select options so colors always match.
export const CATEGORY_COLORS = {
  Development: 'blue',
  Design: 'purple',
  'Client Meeting': 'gold',
};

export const DEFAULT_WEEKLY_TARGET_HOURS = 40;
